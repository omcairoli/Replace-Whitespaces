# Replace-Whitespaces

Whitespace Program.
Method to replace all whitespaces in a sentence with "%20"
User supplies the sentence to test
Returns new string if spaces found, returns "Not found!" if sentence does not contain spaces
Written and tested for Python 2.7
